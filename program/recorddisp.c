#include "liquorSystem.h"

void recorddisp(){
  char consumerName[1000]; // 仮
  printf("出荷実績を表示します。");
  printf("顧客名を入力してください。\n");
  scanf("%c", consumerName);

  if(consumerName == 出荷伝票.名前){
    // 出荷伝票一覧の表示
  }
  else{
    printf("この名前は存在しません")
  }
}
